{
  "name": "create-path-scout-plugin",
  "version": "0.0.7",
  "description": "Scaffold a new path-scout plugin",
  "private": false,
  "type": "module",
  "license": "AGPL-3.0-only",
  "bin": {
    "create-path-scout-plugin": "./dist/index.js"
  },
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "dev": "tsc --build --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@path-scout/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "files": [
    "dist"
  ]
}
